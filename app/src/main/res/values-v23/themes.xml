<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- This theme is for API 23+ -->
    <!-- It can directly inherit from the main app theme -->
    <style name="Theme.AbsensiMahasiswa" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        
        <!-- Re-apply primary and secondary colors for consistency -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>
</resources>